# 기본은 LF. 저장소 안에서는 한 가지 줄바꿈만 산다.
* text=auto eol=lf

# ⚠ PowerShell 은 Windows 전용 도구다. CRLF 로 체크아웃한다.
#   (BOM 과 함께 — PS 5.1 은 BOM 이 없으면 .ps1 을 ANSI 로 읽어 한글이 깨진다)
*.ps1 text eol=crlf

# 바이너리
*.png binary
*.jpg binary
*.zip binary
