# ContextOps 개발 규칙

세션마다 **매번** 읽는 문서다. 그래서 「무엇을 하기 전에 알아야 하는가」만 있다.
상세는 전부 `docs/SPEC.md` 에 있고 여기엔 **문 이름만** 적는다.

---

## 🔴 시작

1. **`docs/feedback/INBOX.md`** — 사용자 지시. 여기 뭐가 있으면 그게 지금의 일이다
2. **`docs/feedback/FINDINGS.md`** — 관통이 찾아 놓은 대장. 보통 일은 여기 맨 위에 있다
3. **`docs/PLAN.md`** — Phase 체크리스트. `- [ ]` 맨 위 하나가 다음 행이다
4. **`docs/STATUS.md`** — 어디까지 했고 무엇에 막혀 있나
5. 검증은 한 줄: `powershell -ExecutionPolicy Bypass -File tools/ci.ps1`

**정본은 `docs/SPEC.md`.** 구현 전에 해당 § 을 읽는다. 색인은 `loop/PROMPT.md` ② 에 있다.

### 🔴 SPEC 을 「현실」로 믿지 마라

SPEC 은 **의도**의 정본이고 **코드가 현실**이다.
SPEC 에 적힌 함수·필드·엔드포인트를 근거로 삼기 전에 **코드에서 그 이름을 찾아
실제로 존재하고 무언가를 하는지 확인해라.** 안 그러면 없는 것 위에 짓는다.

둘이 다르면: **무엇을 만들지는 SPEC · 무엇이 지금 도는지는 코드.**
차이를 발견하면 `docs/feedback/FINDINGS.md` 에 적어라.

---

## 🔴 절대 원칙 P1~P7 — 이게 제품의 전부다

전문은 `docs/SPEC.md` §0.1. 기계 검사는 `tools/principles.ps1`.

| # | 한 줄 |
|---|---|
| P1 | 서버는 저장소 **코드 본문·secret·개인 Memory·대화 transcript** 를 절대 받지 않는다 |
| P2 | **제품 코드**는 사용자의 Claude 를 대신 호출하지 않는다 |
| P3 | 서버측 LLM 은 API 키로만 · 4개 기능 한정 · `withBudget()` 경유 |
| P4 | 승인 이후 파이프라인에 LLM 없음. 같은 snapshot → **byte-identical Pack** |
| P5 | 진행은 **마일스톤 단위만.** 개인 생산성 점수·순위 금지 |
| P6 | Hook 은 파일을 변경하지 않는다 |
| P7 | 모든 Pack 줄은 항목 ID → 원문으로 **역추적**된다 |

### ⛔ P2 의 경계 — `loop/` 와 `tools/` 는 제품이 아니다

**이 저장소는 자율 루프가 만든다.** 그 루프는 `claude -p` 로 돈다.
P2 가 금지하는 것은 **배포되는 제품**(`plugin/` · `packages/` · `apps/`)이
**사용자의** 구독을 대신 쓰는 것이다. `loop/`·`tools/` 는 우리 자신의 구독을 쓰는 개발 도구다.

⚠ **`loop/loop.ps1` 을 「P2 위반」이라며 지우지 마라.** 지우면 다음 바퀴가 없다.

---

## 🧱 코드 — 나중에 더할 수 있게

> **「지금 하나 지우는 데 하루가 걸린다면, 하나 더하는 데도 하루가 걸린다.」**

- **개념 하나 = 정본 파일 하나.** 목록·표를 여러 곳에 복사하지 마라
  (Zod 계약 `packages/schema` · 템플릿 `packages/compiler/templates` · 에러 코드 한 곳)
- **모든 외부 입력은 `packages/schema` 로 파싱한다.** 새 필드는 스키마에 **먼저** 추가한다
- **접근은 한 문으로.** 서버 AI 는 `withBudget()` · 파일 쓰기는 sync 의 atomic 경로 하나
- **수치를 하드코딩하지 마라** — 상수를 참조한다. 같은 값이 두 곳에 있으면 조용히 갈라진다
- **enum 값은 직렬화된다 — 끝에만 더하고 중간을 지우지 마라**
- 지울 때는 **왜 지웠는지 한 줄** 남긴다

### 🔴 확장은 「표에 한 줄」이어야 한다 (사용자 상시 지시)

> *"유지보수, 객체지향, 확장성 생각하면서 코딩해야해"* — **오픈소스로 공개한다.**
> 남이 읽고, 남이 한 줄 더한다.

객체지향은 **클래스를 많이 만드는 것이 아니라, 더하는 자리를 하나로 만드는 것**이다.
판정 기준은 하나다: **「ItemType 을 하나 더하려면 몇 군데를 고쳐야 하나?」**

- **더하는 절차를 그 표 옆 주석에 적어라.**
  예: `// 새 ItemType: ①이 enum ②data 스키마 ③partition 표 ④템플릿 ⑤liveness 테스트`
  ★ 왜 — 절차가 코드 밖(사람 머릿속·PR 설명)에 있으면 **다음 사람은 반드시 하나를 빠뜨린다.**
- **분기(`switch`·`if type ===`)를 여러 파일에 흩지 마라.** 타입별로 달라지는 것은
  **표 하나**(`Record<ItemType, …>`)로 모으고, 화면·컴파일러는 그 표를 **읽기만** 한다.
  ★ 왜 — 그래야 타입이 늘 때 화면을 안 고쳐도 따라온다. 이게 목표 상태다.
- **경계에는 인터페이스를 두되, 사용자가 하나뿐이면 만들지 마라.**
  ★ 왜 — 과설계는 확장성이 아니라 **확장을 막는다.** 추상이 하나뿐인 구현을 위해 있으면,
  둘째가 왔을 때 그 추상이 안 맞아서 결국 다시 짠다.
  **둘째 사용자가 생기면 그때 정본으로 올린다.**
- **의존 방향은 한쪽으로.** `schema ← compiler ← web/plugin`. 스키마가 웹을 import 하면 끝이다.
- **공개 API 는 좁게.** 패키지는 `index.ts` 하나로만 내보낸다 — 내부를 직접 import 하기
  시작하면 리팩터링이 불가능해진다.

### 정의만 있고 아무 일도 안 하는 코드가 이런 저장소의 단골 고장이다

타입은 10종인데 8종을 아무도 안 읽고, 필드는 있는데 늘 빈 배열이다. **화면에는 멀쩡히 뜬다.**

→ **표를 만들면 「표의 항목이 전부 실제로 뭔가를 바꾼다」를 시험으로 잠가라.**
`ItemType` 10종 · `SourceRef` 4종 · 에러 코드 9종 · sync 상태 5종이 전부 이 위험에 있다.

---

## ✅ 퀄리티 게이트

**「테스트 초록」은 완성이 아니다.**

- `tools/ci.ps1` 5층이 전부 초록이어야 커밋한다. 특히 **`principles` 는 타협 없다**
- 화면을 만들면 **캡처로 직접 눈으로 보고** 판정한다. 정본은 `docs/DESIGN_BRIEF.md` §3 토큰.
  참고 시안은 `design/*.dc.html` — **베끼지 말고 참고만** 한다. 시안에 있는데 토큰에 없는 값이
  필요하면 **먼저 토큰으로 추가하고** 쓴다
- **관통 시나리오**(`tools/walkthrough.ps1`)가 지나야 「된다」다
- **같은 지적이 두 번 나오면 규칙이 아니라 게이트로 올려라.**
  `tools/principles.ps1` 이나 테스트로 만들어라 — **게이트는 문서보다 강하다**

⚠ 아직 없는 층은 `SKIP` 이다. **대상이 생겼는데도 SKIP 이면 그게 고장이다** —
그 원칙은 지금 안 지켜져도 초록이라는 뜻이다.

---

## ⚠ 이 환경에서 밟는 함정

- **셸 명령을 `;` 나 `&&` 로 이어 붙이지 마라 — 한 번에 하나씩.**
  무인 세션은 `bypassPermissions` 로 도는데도 여러 명령을 이어 붙이면 **거부된다.**
  조용히 막히는 종류라 로그를 안 보면 모른다
- **PowerShell 은 5.1 이다.** `&&`·`??`·삼항 연산자 없음.
  **네이티브 exe 에 `2>&1` 을 쓰지 마라** — stderr 각 줄이 ErrorRecord 로 감싸져
  **exit 0 인데도 `$?` 가 `$false` 가 된다.** 리다이렉트가 필요하면 `cmd.exe /c` 를 거쳐라
- **`.ps1` 은 UTF-8 BOM 있음으로 쓴다.** BOM 이 없으면 PS 5.1 이 ANSI 로 읽어
  **한글 주석·문자열이 깨진다**
- **`.ci/shots/` 는 관통마다 통째로 지워진다.** 근거 캡처는 **적기 전에** 밖으로 복사해라 —
  고치는 바퀴와 지우는 바퀴가 같은 바퀴다

---

## 🤝 같이 지키는 것

AI 세션 여럿이 이 디렉터리를 공유할 수 있다.

- **커밋을 미루지 마라** — CI 초록 확인 후 바로 커밋
- **`git add .` 금지, 경로로 담는다.** 커밋 전 `git status --short` 로 남의 작업이 섞였는지 본다
- **커밋 단위는 `docs/PLAN.md` 의 한 행** 또는 FINDINGS 한 항목
- **테스트 없는 API·CLI 변경은 머지하지 않는다**
- 커밋 메시지 끝에: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
