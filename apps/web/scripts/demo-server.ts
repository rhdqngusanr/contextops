// =====================================================================
//  `pnpm --filter web demo:db` — 게스트 데모까지 심은 개발용 씨앗 서버
//
//  ★ 왜 파일이 따로 있나 — 환경 변수를 명령줄 앞에 붙이는 문법(`DEV_SEED=demo tsx …`)은
//    **Windows PowerShell 에 없다** (CLAUDE.md 「이 환경에서 밟는 함정」). 스크립트가
//    두 줄이면 어느 셸에서나 같게 돈다. 서버 코드는 `dev-server.ts` 하나뿐이다.
// =====================================================================

export {}

process.env.DEV_SEED = 'demo'
await import('./dev-server')
