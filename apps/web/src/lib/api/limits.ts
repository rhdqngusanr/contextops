// =====================================================================
//  만들 수 있는 개수의 상한 — 표 하나 (INBOX H11 · 2026-09-10)
//
//  ★ 왜 있나 — 공개 배포에서 로그인은 GitHub 계정이면 누구나다. 상한이 없으면 스크립트 하나가 팀·프로젝트를
//    수천 개 만들어 무료 DB 를 채운다. 값은 **대회 심사 규모**에 맞춘 것이지 제품의 요금표가 아니다 —
//    셀프호스팅이면 여기 숫자를 고친다.
//  ★ 서버(라우트)가 세고, 화면은 `REASON_HINT` 로 같은 표의 숫자를 사람 말로 낸다 — 두 곳에 숫자를 적지 않는다.
//  ⚠ 데모 리셋은 옆자리에 한 팀을 더 만든다(`demo-next`) — 데모 owner 의 팀은 그 순간 둘이라 상한 3 안이다.
// =====================================================================

export const CREATION_LIMITS = {
  /** 한 계정이 **owner 로** 가진 팀의 수. member 로 초대받은 팀은 세지 않는다. */
  TEAM_LIMIT: { max: 3, what: '한 계정이 만들 수 있는 팀' },
  /** 한 팀 안의 (지워지지 않은) 프로젝트 수. */
  PROJECT_LIMIT: { max: 5, what: '한 팀의 프로젝트' },
} as const

export type LimitReason = keyof typeof CREATION_LIMITS
