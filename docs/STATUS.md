# STATUS — 다음 바퀴의 유일한 기억

> **매 바퀴 끝에 이 파일을 갱신한다.** 안 쓰면 다음 바퀴는 아무것도 모르는 채로 시작한다.
>
> **한 일이 아니라 잰 것을 써라.**
> 「API 작업함」 ✗ / 「publish 409 재현 테스트 3개 초록, Pack 파일 6개, manifest_hash 고정」 ○

_마지막 갱신: 2026-09-03 · 루프 0바퀴_

---

## 지금 어디인가

**P0 시작 전.** 저장소에는 **루프와 문서만** 있다. 제품 코드는 아직 한 줄도 없다.

| 있는 것 | 없는 것 |
|---|---|
| `loop/` — 자율 루프 일체 (env·loop·ctl·relay·PROMPT·HANDOFF) | `apps/web` · `packages/schema` · `packages/compiler` · `plugin/` |
| `tools/ci.ps1` · `principles.ps1` · `walkthrough.ps1` | `fixtures/` · `pnpm-workspace.yaml` · 테스트 |
| `docs/SPEC.md` · `DESIGN_BRIEF.md` · `PLAN.md` | Supabase 프로젝트 · Vercel 연결 |

→ 검사 층 대부분이 `SKIP` 이다. **P0 단계에서는 정상이다.**
대상이 생기면 SKIP 이 저절로 풀린다 — 안 풀리면 그게 고장이다.

## 다음 바퀴가 할 일

`docs/PLAN.md` **P0 첫 행**: 모노레포 뼈대.
관통 시나리오는 아직 돌 수 없다 (`PROMPT.md` ④0 을 따라 PLAN 에서 고른다).

## 잰 것

**루프 기준선** (2026-09-03 dryrun 2바퀴 · `logs/cycles/2026-09-03_dry00*.jsonl`)

| | 바퀴 1 | 바퀴 2 |
|---|---|---|
| 시간 | 24초 | 21초 |
| 턴 | 7 | 7 |
| 비용 | $0.42 | $0.41 |

두 세션이 **각각 독립적으로** 같은 답을 냈다 — INBOX·FINDINGS 가 비었으니
`PROMPT.md` ④0 경로로 `PLAN.md` P0 첫 행(모노레포 뼈대). 읽는 순서가 실제로 먹는다는 뜻이다.

⚠ 이건 「읽고 판단하는」 부분만 잰 것이다. 진짜 바퀴는 파일을 쓰고 CI 를 돌리고
커밋하고 다시 관통한다. **첫 실주행 로그로 다시 재라** — `CycleTimeoutMin`(45분)과
`MaxCostUsd`($30)를 그 숫자에 맞춘다.

_(제품 코드의 수치는 첫 실주행부터 여기에 남긴다 — 테스트 수, 파일 수, 해시, 초.)_

## 눈 판정 대기

_(없음)_

## 막힌 것 — 🙋 사람이 해야 하는 것

| 무엇 | 왜 루프가 못 하나 | 언제 필요한가 |
|---|---|---|
| Supabase 프로젝트 생성 · 키 발급 | 계정·결제가 필요하다 | P1 시작할 때 |
| Anthropic API 키 (서버측 AI 용, 종량제) | 키 발급은 사람이 | P3 시작할 때 |
| Vercel 프로젝트 연결 · 환경변수 | 계정 연결이 필요하다 | P5 |
| 실데이터 픽스처 공개 가능 여부 판단 | 제품 결정이다 | P5 (안 되면 합성 픽스처만) |
| 🔴 **데모 픽스처 이야기를 무엇으로 할 것인가** (아래) | 제품 결정이다 — 데모 서사 전체가 걸린다 | **P0 마지막 행(픽스처)에 닿기 전** |

⚠ 루프는 위 항목을 **추측으로 진행하지 않는다.** `.env.example` 과 코드 배선까지만 하고
여기 적고 멈춘다.

### 🔴 픽스처 서사가 세 곳에서 갈렸다 (2026-09-03 발견)

같은 「데모에서 무엇을 보여줄 것인가」를 세 문서가 다르게 말한다:

| 어디 | 무엇 |
|---|---|
| `docs/SPEC.md` §10.1 | **paylab** — NestJS 결제 서비스. 재시도 5회/3회 · 환불 SLA · PII 로그 |
| `docs/DESIGN_BRIEF.md` 화면 1·4 | **북스택**(검색 API·카카오·ISBN) 인데 화면 4 예시만 `parking-api` |
| `design/*.dc.html` 시안 | **bookstack** — `BS-M1~M3` · 카카오 API 검증 · ISBN 13자리 |

★ 왜 지금 정해야 하나 — 픽스처는 `PLAN.md` P0 **마지막 행**이고, 정해지면
랜딩 Before/After · 충돌 카드 3장 · 로드맵 마일스톤 ID · 시드 데이터가 **전부 그 서사를 따른다.**
나중에 바꾸면 그 넷을 다시 쓴다.

**추천: bookstack.** 시안과 DESIGN_BRIEF 랜딩이 이미 그걸로 쓰였고(2 대 1),
「카카오 API 실응답을 검증했나」는 **문서↔코드 충돌**이 자연스럽게 나오는 이야기다.
paylab 의 「재시도 5회 vs 3회」도 좋지만 이미 쓰인 화면이 없다.

⚠ **루프는 이걸 추측하지 않는다.** P0 픽스처 행에 닿으면 여기를 보고 멈춘다.
사람이 정하면 `docs/SPEC.md` §10.1 을 고치고 이 절을 지운다.

## 밟은 함정

_(루프가 같은 벽에 두 번 부딪히면 여기 적는다. 두 번 나온 것은 `loop/PROMPT.md` ③ 의
규칙으로, 기계가 잴 수 있으면 `tools/principles.ps1` 의 검사로 올린다.)_
