# 79바퀴 · 2026-09-06 — 서버측 AI 를 Anthropic → Gemini 로 (INBOX 지시)
# 전부 진짜 API(gemini-3.5-flash · 우리 키 · .env.local)에 대고 잰 것. 키·프롬프트·응답 본문은 안 적었다.

## 1. 어느 칸이 스키마를 받나 (apps/web 에서 임시 스크립트 gemini-diag-79.ts · 지웠다)

A responseJsonSchema + AiStructureOutput 전체(11,485b)  : 400 "Request contains an invalid argument."
B responseSchema     + 전체                              : 400 Unknown name "$schema" … Unknown name "$ref" at …items (OpenAPI 부분집합 — $ref 를 못 받는다)
C responseJsonSchema + {colors: string[]}                : 200 candidates,usageMetadata,modelVersion,responseId
D responseSchema     + {colors: string[]}                : 200

## 2. 전체 스키마에서 무엇을 벗겨야 A 가 200 이 되나 (gemini-diag2-79.ts · 지웠다)

누적:
  0 원본                        11485b → 400
  1 -$schema                    11428b → 400
  2 +inline $ref                22098b → 400
  3 +const→enum                 22108b → 400
  4 +oneOf→anyOf                22108b → 400
  5 -pattern/format             15553b → 400
  6 -additionalProperties       14277b → 400
  7 -min/max 여섯               10800b → 200   ← 여기서 뒤집힌다
  8 -default                    10110b → 200
낱개(원본에서 그것만):
  a -minLength/maxLength        10728b → 400
  b -minimum/maximum            11381b → 400
  c -minItems/maxItems          11293b → 200   ← 범인은 이 둘뿐
  d -여섯 전부                   10432b → 200
  e -$schema -여섯               10375b → 200
→ `$schema`·`$defs`·`$ref`·`const`·`oneOf`·`pattern`·`format`·`additionalProperties`·`minLength`·`maxLength`·`minimum`·`maximum`·`default` 는 전부 받는다.
  **`minItems`·`maxItems` 만** 이유 없이(「invalid argument」) 거절한다. → `client.ts` `GEMINI_UNSUPPORTED_SCHEMA_KEYWORDS` 두 줄.

## 3. callModel() 끝까지 (scripts/ai-smoke.ts = `pnpm --filter web ai:smoke`)

const 그대로 보냈을 때 (minItems/maxItems 만 벗김):
  {"model":"gemini-3.5-flash","latencyMs":11176,"inputTokens":83,"outputTokens":366,"valueType":"object","items":2,"openQuestions":0,
   "zodOk":false,"zodIssues":["items.0.type: Invalid discriminator value. Expected 'mission' | 'goal' | … ","items.1.type: Invalid discriminator value …"]}
  → 200 이고 JSON 이지만 `type` 이 표 밖 — `const` 를 받기만 하고 **지키지 않는다.**

const → enum 으로 바꿔 보냈을 때 (`toGeminiSchema()` 최종):
  {"model":"gemini-3.5-flash","schemaKeys":["$schema","type","properties","required","additionalProperties","$defs"],"hasDefs":true,
   "latencyMs":12309,"inputTokens":83,"outputTokens":248,"valueType":"object","items":2,"itemTypes":["policy","policy"],"openQuestions":0,"zodOk":true,"zodIssues":0}
  → 문서 두 문장에서 policy 2 · Zod 통과 · 토큰은 usageMetadata 에서.

## 4. P3 게이트가 무나 (tools/principles.ps1 · generateContent 를 패턴에 더한 뒤)

apps/web/src/lib/ai/rogue-gate-probe.ts (withBudget 없이 …:generateContent 를 fetch 하는 척) 를 두고:
  P3   FAIL  모든 LLM 호출이 withBudget() 경유
             apps\web\src\lib\ai\rogue-gate-probe.ts
  principles: OK 8 · SKIP 0 · FAIL 1
지운 뒤: P3 OK · 2개 호출부(client.ts · budget.ts — 둘 다 예외 자리) · principles: OK 9 · SKIP 0 · FAIL 0
⚠ 첫 시도의 probe 는 주석에 「withBudget 없이」라고 적어서 게이트가 정당하게 통과시켰다 — 낱말 하나가 게이트를 만족시킨다는 뜻이다(문자열 검사의 한계 · 알고 있으라).

## 5. 시험

ai-budget · ai-conflict · ai-structure · ai-job · web-item-doors · readme : 6 파일 154/154 (스텁을 test/helpers/ai.ts 한 곳으로)
ai-client (새) : 9/9 — 못 받는 키워드가 모든 깊이에서 빠졌나 · const→enum · 나머지 키워드는 그대로 · 순수 · JSON 아니면 undefined · 키 없으면 죽는다
