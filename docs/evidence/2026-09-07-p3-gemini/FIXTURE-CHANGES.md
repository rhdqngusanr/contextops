# 이 근거가 가리키는 픽스처의 변경 이력

이 폴더의 probe 들은 `fixtures/paylab-docs/goals.md` · `old-roadmap.md` 의 **글자 위치(offset)** 를 가리킨다.
픽스처가 바뀌면 그 offset 이 다른 글자를 가리킬 수 있으므로, 바꾼 것을 여기 적는다.

| 날짜 | 무엇 | offset 에 미치는 영향 |
|---|---|---|
| 2026-09-12 계획 → **2026-09-09 실행** | `goals.md` 의 기한 문자열을 **같은 길이**로 교체 — M1 `2026-04-30`→`2026-10-31` · M2 `2026-05-29`→`2026-11-30` · M3·G1~G3 `2026-06-30`→`2026-12-31` (심사일 기준 전부 지난 기한이라 Roadmap 이 「기한 지남」으로 읽혔다 · INBOX 블로커 5) | **없음** — 10자 → 10자라 모든 offset 이 그대로다. probe 의 「offset 범위 안 20/20 · 6/6」 판정은 여전히 유효하고, 그 범위를 잘라 보면 날짜만 새 값이다 |
| 2026-09-09 | `probe-87-run3.json` 의 충돌 5장 중 3장을 데모 씨앗에 **기록물**로 심었다 (`apps/web/src/lib/demo/seed.ts` 의 `RECORDED_CONFLICTS`). 질문 문장은 그대로, 항목 id 만 씨앗의 id 로 이었다. 상대편(옛 규칙)은 `old-roadmap.md` 「운영 규칙」 셋을 `draft` 항목으로 심어 실재하게 했다(`STALE_RULES`) | — (근거 파일은 안 바뀌었다) |
| 2026-09-11 | `RECORDED_CONFLICTS[].question` 의 따옴표 안 **항목 이름 5곳**을 씨앗의 지금 제목으로 바꿨다 — 「외부 PSP 호출 최대 5회 재시도」→「결제사 호출 재시도는 5번까지, 간격은 점점 늘려서」 · 「PSP 호출 재시도 규칙」→「(옛 문서) 결제사 재시도는 3번, 0.5초 간격 고정」 · 「환불 24시간 이내 종결」→「환불은 24시간 안에 종결한다」 · 「로그에 개인정보(PII) 기록 금지」→「기록(로그)에 개인정보를 남기지 않는다」 · 「웹훅 원본 payload 로그 보관 규칙」→「(옛 문서) 결제사 알림 원본을 기록에 7일 보관」 (씨앗 제목을 사람 말로 바꾼 뒤 한 카드 안에서 같은 규칙이 이름 둘로 불렸다). 문장의 나머지는 모델이 낸 그대로 · 원문은 `probe-87-run3.json` `conflict.conflicts[].question`. 카드 끝의 기록물 각주(`RECORDED_CONFLICT_NOTE`)도 모델 이름·파일 경로 없이 사람 말로 | — (근거 파일은 안 바뀌었다) |
