# 83바퀴 · 2026-09-07 (KST 00:25~00:27) — FINDINGS 143 을 고친 뒤 PLAN P3 첫 행을 진짜 Gemini 로 다시 잰다
# 전부 진짜 API(gemini-3.5-flash · 우리 키 · .env.local) · DB 는 PGlite(freshDb) · 길은 제품 그대로 · 코드 5be2611
# 문: `pnpm --filter web p3:measure` — **같은 코드로 두 번** 돌렸다. 1회차 = probe-run1.json · 2회차 = probe.json.
# (폴더 이름의 날짜는 스크립트의 로컬 날짜 · probe.json 의 measured_at 은 UTC 라 09-06 으로 적혀 있다)
# 81·82바퀴의 기록은 옆 폴더 ../2026-09-06-p3-gemini/probe.txt §0~§5.

## 0. 무엇을 바꿨나 (FINDINGS 143 · 프롬프트만)

structure.ts SYSTEM 에 RULE_LIST_LINES 두 줄:
  - 규칙을 나열한 절(「운영 규칙」·「지켜야 할 것」 같은 목록)은 **줄마다** 항목 하나다. 문서 종류가 로드맵·메모여도 그 안의 규칙은 policy 로 낸다.
  - 「하지 않는다」·「기한은 따로 두지 않는다」 같은 **부정형도 규칙**이다. 숫자·기한이 없다고 빼지 않는다.
conflict.ts · 스키마 · 화면은 그대로. 기준(항목 12 · 충돌 3)은 안 낮췄다.

## 1. 완료 기준 대 실측 — 두 번

| 기준 | 82 (3b6ebef) | 83 · 1회차 | 83 · 2회차 | 판정 |
|---|---|---|---|---|
| old-roadmap → 항목 | 5 (roadmap 3 · policy 2) | **6** (roadmap 3 · policy 3) | **6** (roadmap 3 · policy 3) | 환불 줄이 policy 로 ✅ (143 의 합격) |
| goals.md → 항목 12 | 12 · 질문 4 | **18** (goal 3 · policy 7 · roadmap 3 · architecture 5) · 질문 4 | **14** (goal 3 · policy 7 · roadmap 3 · domain 1) · 질문 4 | ✅ |
| 충돌 3 | 1 | **0** (후보 {used 0, total 0}) | **3** (후보 {3, 3} · 전부 contradiction high) | ✗ 실행마다 갈린다 → 146 |
| 모든 호출이 withBudget 경유 | 3행 | 3행 (structure 1,031/1,750 · 2,591/5,305 · conflict 1,721/10 · ≈ $0.019) | 3행 (1,031/1,737 · 2,591/4,664 · conflict 1,619/371 · ≈ $0.019) | ✅ |
| 인용이 문서 범위 안 · 그 문장 | 21/21 | 24/24 (goals 22 · roadmap 6) · 눈으로 24 | 24/24 (18 · 6) · 눈으로 24 | ✅ |
| accept 거절 | 1 (item_psp_retry_policy) | 1 (index 3 · 같은 slug) | 1 (index 3 · 같은 slug) | 145 그대로 |

## 2. 환불 줄 — 두 번 다 항목이 됐다 (probe*.json 의 roadmap.items)

1회차  item_refund_handling_sla   policy  520-559  "- 환불은 담당자가 확인하는 대로 처리한다. 기한은 따로 두지 않는다."
2회차  item_refund_sla_none       policy  522-559  "환불은 담당자가 확인하는 대로 처리한다. 기한은 따로 두지 않는다."   (scope project)
나머지 둘도 그대로: PSP 재시도 3회/0.5초 고정 (452/454-490) · 웹훅 원본 payload 로그 7일 (560/562-603).
→ 82 까지 policy 2 이던 것이 3 이 됐다. 프롬프트 두 줄이 한 일이다. 「지어낸」 항목은 없다 — 셋 다 원문 한 줄씩이다.

## 3. 2회차의 충돌 셋 (probe.json 의 conflict.conflicts · 전부 질문형 · 판정 없음)

contradiction high  item_log_pii_prohibited           vs item_webhook_payload_log   「새 항목은 PSP 원본 payload 를 로그에 남기지 못하게 금지 … 기존은 장애 조사를 위해 남기도록 …」
contradiction high  item_prohibit_fixed_interval_retry vs item_psp_retry_policy      「새 항목은 고정 간격 재시도를 금지 … 기존은 0.5초 고정 … 어느 정책을 적용해야 합니까?」
contradiction high  item_refund_sla_limit             vs item_refund_sla_none        「새 항목은 환불 요청을 24시간 이내에 종결 … 기존은 처리 기한을 따로 두지 않는다 …」
⚠ 둘째 짝의 goals 쪽이 「외부 PSP 호출 재시도 규칙」이 아니라 「고정 간격 재시도 금지」다 — 재시도 규칙 후보는 같은 slug 라 accept 에서 거절됐다 (145).
   goals.md 에 「고정 간격 재시도는 금지한다」 줄이 따로 있어서 짝이 섰다. 145 를 안 고치면 이 짝도 모델이 그 줄을 항목으로 내느냐에 달려 있다.

## 4. 1회차는 왜 0 인가 — 후보가 0 이다 (FINDINGS 146)

conflict.ts detectConflicts(): 후보 = 같은 type(DB) 이고 scopeKey 가 같은(코드) active 항목. scope 는 모델이 항목마다 고른다.
  1회차  candidates {used 0, total 0} · conflict 호출은 갔다(장부 1,721/10 토큰 · 항목 17개끼리만) · 충돌 0
         probe 가 scope 를 안 적어 로드맵 policy 셋과 goals policy 일곱 중 어느 쪽이 갈렸는지 **못 봤다**.
  2회차  roadmap scopes { path:src/webhook/ 1 · path:src/ledger/ 1 · path:src/psp/ 1 · project 3 } — policy 셋이 project
         goals   scopes { project 10 · path:src/payment/ 1 · path:src/refund/ 1 · path:src/common/ 1 · domain:payment 1 } — policy 일곱이 project
         → policy 끼리 type·scope 가 같아 후보 3 (로드맵 policy 셋) · 충돌 3.
→ 같은 코드·같은 프롬프트에서 3 ↔ 0. 「충돌 3」은 145(slug) 와 146(scope) 을 닫아야 안정된다. p3-measure.ts 는 이제 itemScopes 를 적는다.

## 5. 이번 바퀴가 안 한 것

- 145 · 146 은 안 고쳤다 (한 바퀴에 하나).
- 화면 3(/import) 을 브라우저로 본 적은 여전히 없다 — STATUS 「눈 판정 대기」.
- PLAN P3 첫 행은 열어 뒀다 — 「두 번 연속 3/3」 이 체크 조건이다.

# 84바퀴 · 2026-09-07 (KST 00:40~01:05) — FINDINGS 145 를 닫고 재려다 147 을 찾아 같이 닫았다
# 전부 진짜 API(gemini-3.5-flash · 우리 키) · PGlite(freshDb) · 길은 제품 그대로 · 문 `pnpm --filter web p3:measure`
# 파일: probe-run2.json = 83바퀴 2회차(옛 probe.json 을 옮김) · probe-84-fail.json = 145 만 고친 코드(9a5da46)의 실패
#       probe-84-run1.json · probe-84-run2.json(= probe.json) = 145+147 을 고친 코드(147 커밋)로 두 번

## 0. 무엇을 바꿨나
- 145 (9a5da46): 러너가 프로젝트의 context_items.public_id 전부를 structureDocument({takenIds}) 로 넘기고 uniqueId() 가 그것으로 시작한다. 프롬프트 불변.
- 147 (다음 커밋): toSourceRef 가 indexOf 대신 findFolded — 공백 연속(줄바꿈 포함)을 공백 하나로 접어 찾고 offset 은 원문 자리로 되짚는다.

## 1. 145 만 고친 코드로 두 번 — goals.md 가 두 번 다 AI_OUTPUT_INVALID (probe-84-fail.json 은 둘째)
  roadmap: 6 항목(policy 3) · 6/6 · 활성 6            goals: failed AI_OUTPUT_INVALID · 36초 · 31초 · 장부 출력 0(실패 행은 추정치)
  일회용 진단(scripts/p3-diag-84.ts · 지움)으로 goals.md 조각 하나를 직접 불렀다 — 응답은 finishReason STOP · JSON 파싱 OK · 인용 27개 중 **5개**가 원문에 없음 · 여러 곳 0:
    item_paylab_mission            「가맹점이 우리를 쓰는 이유는 하나다 — PSP 가 …」        원문은 「이유는」 뒤에 줄바꿈
    item_rule_exponential_backoff  「재시도 간격은 **지수 백오프**로 1초 → …」              원문은 「백오프**로」 뒤에 줄바꿈
    item_rule_retry_conditions     「… 4xx 는 재시도하지 않는다. 멱등키(…」                  원문은 「않는다.」 뒤에 줄바꿈
    item_rule_refund_escalation    「… **자동으로 승인 대기 큐에서 빠져 에스컬레이션**된다. …」 원문은 「빠져」 뒤에 줄바꿈
    item_rule_no_pii_in_logs       「… 에러 리포트· 웹훅 수신 덤프 …」                       원문은 「리포트·」 뒤에 줄바꿈 → 모델은 공백
  재시도(불평은 첫 항목 하나만): item_paylab_mission 을 「… — **PSP 가 … 것.**」 으로 굵게까지 더해 다시 없음 → 2회 실패.
  → 다섯 다 「줄 중간 하드 줄바꿈을 공백으로」다. 글자를 바꾼 게 아니다. 83바퀴의 24/24 는 인용이 전부 한 줄 안이었던 우연.

## 2. 145+147 코드로 두 번 — 완료 기준 대 실측
  기준                      1회차 (probe-84-run1.json)                2회차 (probe-84-run2.json = probe.json)
  goals.md 항목 ≥ 12        24 (policy 10 · arch 5 · goal 3 · roadmap 3 · mission/domain/constraint 1)   18 (policy 10 · goal 3 · roadmap 3 · arch 2)
  열린 질문                 4                                          4
  인용 (goals · roadmap)    28/28 · 6/6                                22/22 · 6/6
  accept 거절 (145)         0 (24/24 들어감)                           0 (18/18)
  탐지 후보 {used,total}    3/3                                        3/3
  충돌 ≥ 3                  **4** (contradiction high ×4)              **4** (contradiction high ×4)
  장부                      3행 · 왕복 4 (goals 재시도 1 · 입력 5,257)  3행 · 왕복 3 (재시도 0 · 입력 2,591)
  goals 시간                47초                                       20초
  충돌 짝(두 번 같음): 환불 24h vs 기한 없음 · 고정 간격 금지 vs 0.5초 고정 · PII 금지 vs payload 로그 7일 · 재시도 5회 vs 3회 — 의도된 셋(SPEC §10.1) + 하나.
  scope: goals policy 는 두 번 다 전부 project · 로드맵 policy 셋 project → 후보 3 (146 은 이번엔 안 갈렸다 · 여전히 열려 있다).
  slug: 두 번 다 goals 가 로드맵과 다른 slug(item_paylab_policy_psp_retry_limit · item_psp_retry_limit)를 골라 _2 는 실제로 안 밟혔다 — 밟히는 경우는 시험이 잠근다.

## 3. 이번 바퀴가 안 한 것
- 146 은 안 고쳤다 (적기만). PLAN P3 첫 행은 두 번 연속 3 이상을 봤지만 146 이 열려 있어 아직 열어 둔다.
- 재시도 불평이 첫 인용 하나만 말한다 — 다섯이 틀리면 다섯을 다 말해야 한 번에 고친다. 이번엔 접기로 다섯이 다 지나서 안 건드렸다.
- 화면 3(/import) 은 여전히 브라우저로 안 봤다.

# 85바퀴 · 2026-09-07 (KST 01:05~01:35) — FINDINGS 146 을 닫고 재다가 148 을 찾아 같이 닫았다 · PLAN P3 첫 행을 닫는다
# 전부 진짜 API(gemini-3.5-flash · 우리 키) · PGlite(freshDb) · 길은 제품 그대로 · 문 `pnpm --filter web p3:measure`
# 파일: probe-85-run1.json · probe-85-run2.json · probe-85-fail.json = 146 만 고친 코드(f487d67) 세 번(셋째가 실패)
#       probe-85b-run1.json · probe-85b-run2.json(= probe.json) = 146+148 을 고친 코드(b1f7d3c)로 두 번

## 0. 무엇을 바꿨나
- 146 (f487d67): detectConflicts() 가 후보를 「같은 type」까지만 고른다 — wantedScopes 조건을 뺐다. scope 는 renderItem 의 scope= 줄에 실려 모델이 견준다.
  같은 커밋에서 p3:measure 가 진짜 transport 를 기록 껍데기로 감싼다(왕복마다 finishReason · 글자수 · JSON 여부 · 재시도면 그 불평). 응답 본문은 안 남긴다.
- 148 (b1f7d3c): findFolded 가 강조 표시 `*` 를 양쪽에서 뺀다 (QUOTE_FOLDED_CHARS). 글자·문장부호는 그대로.

## 1. 146 만 고친 코드로 세 번
  기준                      run1 (probe-85-run1)         run2 (probe-85-run2)         run3 (probe-85-fail)
  goals.md 항목 ≥ 12        11 (goal 3 · policy 5 · roadmap 3)   14 (policy 7 · goal 3 · roadmap 3 · domain 1)   failed AI_OUTPUT_INVALID (42초)
  왕복                      2 — 첫 응답 12,816자 JSON, 그러나 items.0~2.id 가 /^item_[a-z0-9_]{3,40}$/ 위반 → 재시도 7,031자 (항목이 줄었다 → 149)   1   2 — 첫 응답 16,446자 JSON · 「item_paylab_mission 의 span.quote 가 조각 원문에 없다: "가맹점이 우리를 쓰는 이유는 하나다 — PSP 가 흔들려도 결제가 흔들리"」 → 재시도도 같은 자리
  인용                      15/15 · 6/6                  18/18 · 6/6                  0/0 · 6/6
  accept 거절               0                            0                            —
  탐지 후보 {used,total}    **6/6**                      **6/6**                      —
  충돌 ≥ 3                  **3** (contradiction high)   **5**                        —
  장부                      3행 ≈ $0.029                 3행 ≈ $0.019                 2행
  후보가 3 → 6 인 것이 146 이다: goals 의 roadmap 항목(scope project)이 old-roadmap 의 마일스톤(path:src/…)과 처음으로 짝이 됐다 (같은 type 이면 후보).
  run3 의 원인: 픽스처 「하나다 — **PSP 가 흔들려도 결제가 흔들리지 않는 것.**」을 모델이 `**` 없이 인용했다. 84바퀴의 재시도는 반대로 `**` 를 더했다. → 148.
  ★ 왕복 기록이 없었으면 84바퀴처럼 일회용 진단을 또 만들었어야 했다 — 이번엔 probe 에 이유가 그대로 있었다.

## 2. 146+148 코드로 두 번 — 완료 기준 대 실측 (PLAN P3 첫 행)
  기준                      run1 (probe-85b-run1)                                  run2 (probe-85b-run2 = probe.json)
  goals.md 항목 ≥ 12        **17** (policy 10 · goal 3 · roadmap 3 · domain 1)      **25** (policy 12 · architecture 5 · goal 3 · roadmap 3 · mission 1 · domain 1)
  열린 질문                 4                                                      4
  인용 (goals · roadmap)    21/21 · 6/6                                            29/29 · 6/6
  재시도                    0                                                      0
  accept 거절               0                                                      0
  scope                     goals 의 roadmap 셋이 path:src/payment/·refund/·common/ · domain 하나 domain:payment · 나머지 project   전부 project (roadmap 의 마일스톤은 path:paylab-api/src/…)
  탐지 후보 {used,total}    **6/6**                                                **6/6**
  충돌 ≥ 3                  **4** (contradiction high ×4)                          **5** (contradiction high ×5)
  장부                      3행 ≈ $0.021                                           3행 ≈ $0.024
  충돌 짝 run1: 환불 24h 에스컬레이션 vs 기한 없음 · PII 금지 vs payload 로그 · 고정 간격 금지 vs 0.5초 고정 · 5회 vs 3회
  충돌 짝 run2: 환불 SLA vs 기한 없음 · 고정 간격 금지 vs 0.5초 · PII vs payload 로그 · 5회 vs 3회 · pending 금지 vs 기한 없음 — 의도된 셋(SPEC §10.1)이 두 번 다 들어 있다.
  → 두 번 연속 항목 ≥ 12 · 충돌 ≥ 3 · 후보 ≥ 3 · 인용 전부 원문 · 장부 = 왕복. **PLAN P3 첫 행을 닫는다.**
  run1 은 146 이 실제로 갈린 실행이다 — goals 의 roadmap·domain 넷이 project 가 아닌데도 후보 6 (83바퀴 코드였다면 roadmap 은 짝이 없었다).

## 3. 이번 바퀴가 안 한 것
- 144(finishReason · 429 → RATE_LIMITED)는 그대로다 — 타입에 finishReason 칸만 더했고 callModel 은 아직 안 읽는다.
- 149(첫 응답 id 패턴 위반 → 재시도 → 항목이 준다 · 재시도 불평이 첫 오류 하나만 말한다)는 적기만 했다.
- 화면 3(/import) 은 여전히 브라우저로 안 봤다.

# ===================================================================== 86바퀴 (2026-09-07 01:5x)
# 전부 진짜 API(gemini-3.5-flash · 우리 키) · PGlite(freshDb) · 길은 제품 그대로 · 문 `pnpm --filter web p3:measure`
# 파일: probe-86-fail.json = 144 만 고친 코드(2c69316) 한 번(실패)
#       probe-86-run1.json · probe-86-run2.json(= probe.json) = 144+150 을 고친 코드(4d8ea74)로 두 번

## 0. 무엇을 바꿨나
- 144 (2c69316): callModel() 이 finishReason 을 읽어 truncated 를 낸다 · 두 재시도 루프가 Zod 앞에서 그것부터 봐 OUTPUT_TRUNCATED_COMPLAINT(「더 짧게」)로 재시도 ·
  geminiTransport 의 429 는 GEMINI_HTTP_ERROR_CODES 표로 ApiError RATE_LIMITED (401/403·5xx 는 Error → INTERNAL).
- 150 (4d8ea74): QUOTE_FOLDED_CHARS 에 백틱 — 인라인 코드 표시도 꾸밈이지 글자가 아니다.

## 1. 144 만 고친 코드로 한 번 — goals.md 가 AI_OUTPUT_INVALID (probe-86-fail)
  goals.md          failed AI_OUTPUT_INVALID (31초) · 왕복 2 · 둘 다 STOP · JSON (10,532자 · 10,303자) — 144 의 잘린 길이 아니다
  불평              「item_g2_refund_sla 의 span.quote 가 조각 원문에 없다: "환불 접수→종결 24시간 이내 95% | refund.closed_at "」
  원인              픽스처 G2 행 「| `refund.closed_at - refund.created_at` p95 |」의 백틱을 모델이 뺐다 → 150 (148 의 `**` 와 같은 종류)
  old-roadmap       6 항목 · 6/6 · 장부 2행(실패 행은 추정치 · 출력 0)

## 2. 144+150 코드로 두 번
  기준                      run1 (probe-86-run1)                                   run2 (probe-86-run2 = probe.json)
  goals.md 항목 ≥ 12        **failed AI_OUTPUT_INVALID** (31초 · 왕복 2 · STOP · JSON 11,487자 · 9,169자)   **23** (policy 8 · architecture 5 · goal 3 · roadmap 3 · constraint 2 · mission 1 · domain 1)
  불평 (run1)               「item_webhook_signature_verification 의 span.quote 가 조각 원문에 없다: "웹훅은 서명 검증 후에만 처리한다. 서명 검증 전에는 payload 를 "」
                            → 픽스처는 「### 3.5 웹훅은 서명 검증 후에만 처리한다」 제목 줄 + 빈 줄 + 「서명 검증 전에는 …」 문단. 모델이 제목을 벗기고 **마침표를 더해** 이었다 → 151 (글자를 더한 것 · 접기로 못 고친다)
  인용 (goals · roadmap)    0/0 · 6/6                                              **27/27** · 6/6
  재시도                    1 (그리고 둘째도 죽음)                                  0
  accept 거절               —                                                      0 (23/23)
  scope                     —                                                      전부 project
  탐지 후보 {used,total}    —                                                      **6/6**
  충돌 ≥ 3                  —                                                      **4** (contradiction high ×4)
  finishReason              STOP · STOP                                            STOP (roadmap 6,374ms · goals 22,726ms · conflict 2,801ms)
  장부                      2행                                                    3행 ≈ $0.024 (structure 4,447 + 17,755 · conflict 2,120 micros)
  → 144 의 두 길(MAX_TOKENS · 429)은 세 실행 어디에서도 안 밟혔다 — 스텁 시험으로만 잠갔다. 「잘리면 더 짧게」가 진짜 모델에서 어떻게 되는지는 아직 실측이 없다.
  → 인용 실패는 세 실행 중 둘 — 150 은 닫혔고 151 은 프롬프트의 일이다. 두 번 연속 기준은 이번엔 못 봤다 (run2 만 통과).

## 3. 이번 바퀴가 안 한 것
- 151 은 적기만 했다 (SYSTEM 한 줄 + 149 ② 의 불평 접기).
- 149 는 그대로다.
- 화면 3(/import) 은 여전히 브라우저로 안 봤다.
