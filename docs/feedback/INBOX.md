# INBOX — 사람이 루프에게

> **루프가 매 바퀴 제일 먼저 읽는 파일이다.** 여기 뭐가 있으면 그게 이번 바퀴의 일이고,
> `PLAN.md`·`FINDINGS.md` 보다 위다.
>
> 사람은 여기에 적기만 하면 된다. 루프가 처리하면 **「끝난 것」으로 옮기고** 커밋 해시를 붙인다.
>
> ⚠ **루프도 이 파일을 고친다.** 그래서 사람이 미리 적어 둔 것이 루프의 커밋에 덮일 수 있다.
> 밤새 돌릴 지시는 여기 말고 `loop/HANDOFF.md` 에 적어라 — `relay.ps1` 이 1차 루프가
> **죽은 뒤에** 꽂아 넣어서 경쟁이 없다.

## 할 것

### 🔴 픽스처 서사는 「결정 대기」가 아니다 — 사람이 잘못 적었다 (2026-09-03)

**`docs/STATUS.md` 「막힌 것」에서 「픽스처 서사가 세 곳에서 갈렸다」 절과
그 위 표의 해당 행(🔴 데모 픽스처 이야기…)을 지워라.** 막힌 것이 아니다.

★ 무엇이 틀렸나 — `design/*.dc.html` 시안의 bookstack(`BS-M1~M3` · 카카오 API · ISBN)은
**목업에 채워 넣은 예시 문구**다. `DESIGN_BRIEF` 가 "lorem 금지, 실제 문구를 쓰라"고 해서
디자이너가 구체적인 이름을 채운 것이지 **픽스처를 정한 게 아니다.**
픽스처 정본은 `docs/SPEC.md` §10.1 **(paylab) 하나**다 — 거긴 TS 40파일 · 의도된 어긋남 3곳 ·
기대 결과(충돌 3 · open_question 4 · M1~M3)까지 적힌 **빌드 스펙**이다.
`docs/PLAN.md` P0 마지막 행도 이미 paylab 이라고 적혀 있다. 그대로 간다.

**대신** `docs/DESIGN_BRIEF.md` 에서 예시 문구가 나오는 자리(화면 1 Before/After 의 북스택 ·
화면 4 의 `parking-api`)에 **주의 한 줄**을 넣어라:

> ⚠ 이 절의 예시 문구는 **목업용**이다. 픽스처 정본은 `docs/SPEC.md` §10.1(paylab)이니
> 화면을 만들 때 **paylab 데이터로 채운다.**

★ 왜 STATUS 가 아니라 거기냐 — 실수는 「화면을 만들며 시안 문구를 그대로 베낄 때」 난다.
**경고는 실수가 나는 자리에 있어야 읽힌다.**

⚠ `FINDINGS.md` 에는 넣지 마라. 거기 맨 위 항목은 다음 바퀴의 일이 되는데,
화면이 아직 없어서 **지금은 할 수 없는 일**이다. 맨 위에 올라가면 바퀴 하나를 헛돌린다.

⚠ 이건 **문서 두 곳을 고치는 작은 일**이다 (PLAN 한 행보다 작다).
이것부터 처리하고 **같은 바퀴에서 `docs/PLAN.md` 의 다음 행을 이어서** 해라.
끝나면 이 항목을 「끝난 것」으로 옮기고 커밋 해시를 붙여라.

## 끝난 것

_(아직 없음)_
