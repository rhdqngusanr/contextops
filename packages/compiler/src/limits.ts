// =====================================================================
//  분량 한도 — 정본은 docs/SPEC.md §4.1 5단계.
//  ★ 왜 상수인가 — 같은 숫자가 컴파일러·테스트·화면에 각각 적히면 한쪽만 고쳐지고
//    조용히 갈라진다 (CLAUDE.md 「수치를 하드코딩하지 마라」).
// =====================================================================

/** CLAUDE.md 가 이보다 길면 정책·제약을 `.claude/rules/policies.md` 로 옮긴다. */
export const CLAUDE_MD_MAX_CHARS = 12_000

/** `.claude/rules/*` 한 파일이 이보다 길면 파트로 나눈다. */
export const RULES_MAX_CHARS = 30_000
