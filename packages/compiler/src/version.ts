// =====================================================================
//  컴파일러 버전 — Manifest 의 `compiler_version` 이 이 값이다 (SPEC §3 · §4).
//
//  ★ 왜 상수 파일 하나인가 — 발행 트랜잭션(SPEC §2.1 5단계)이 이 값을 Manifest 에
//    박아 넣는다. 서버가 `'0.1.0'` 을 손으로 적으면, 컴파일러를 고쳐도 발행된
//    Pack 은 옛 번호를 달고 나간다. 그러면 「어느 컴파일러가 이 Pack 을 만들었나」에
//    답할 수 없고, 재현 주장(P4)의 절반이 사라진다.
//    (CLAUDE.md 「수치를 하드코딩하지 마라 — 상수를 참조한다」)
//
//  ⚠ 올리는 때 — 같은 snapshot 에서 **나오는 Pack 이 달라지는** 변경이면 올린다.
//    템플릿의 모양만 바뀐 것은 `TEMPLATE_VERSION` 이다. 둘은 따로 움직인다.
// =====================================================================

//  0.2.0 (2026-09-06) — Manifest 의 마일스톤이 `due` 를 나른다 (FINDINGS 111). Pack 파일 본문은 그대로
//      (roadmap 절은 전부터 `due:` 를 적었다) · `manifest_hash` 도 그대로(`files` 만 센다) — 달라지는 것은
//      `manifest.json` 의 milestones 뿐이다. 그래도 올리는 이유: 같은 snapshot 에서 나오는 **Manifest** 가 다르다.
export const COMPILER_VERSION = '0.2.0'
