{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": [
    "🔴 include 는 폴더 이름을 세지 않는다 — 글로브로 전부 덮는다 (FINDINGS 92).",
    "  ★ 왜 — 폴더 목록을 손으로 적으면 새 폴더를 더한 사람이 한 줄을 잊고,",
    "    그 폴더는 **조용히 타입 검사 밖**에 선다 (apps/web 의 scripts/ 가 그랬다).",
    "    검사에서 빼는 것은 exclude 에 적어라 — 빼는 것은 눈에 보여야 한다."
  ],
  "extends": "../../tsconfig.base.json",
  "include": ["**/*.ts", "**/*.tsx"]
}
