{
  "_comment": [
    "docs/SPEC.md §8.1 · §8.6. 🔴 여기 적은 훅은 사용자가 세션을 열 때마다 묻지 않고 돈다 —",
    "그래서 P6(훅은 파일을 변경하지 않는다)이 이 파일의 전제다.",
    "⚠ 아직 만들지 않은 스크립트를 여기 적지 마라. 없는 파일을 가리키면 사용자는",
    "  매 세션 오류 줄을 본다. SPEC §8.1 에는 Stop 훅도 있고 그건 docs/PLAN.md P2 셋째 행이다.",
    "★ 훅을 더하는 절차: ① scripts/<이름>.mjs ② 이 표에 한 줄 ③ tools/principles.ps1 의",
    "  P6 검사 대상에 그 파일을 더한다 ④ test/hooks.test.ts 가 둘이 같은지 잰다."
  ],
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/session-start.mjs\"",
            "timeout": 3
          }
        ]
      }
    ]
  }
}
